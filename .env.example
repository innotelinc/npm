# Copy to .env and set real values. Used by the root docker-compose.yml.
DB_MYSQL_PASSWORD=change-me
DB_ROOT_PASSWORD=change-me-too

# Optional basic auth for the backup UI (http://<host>:82).
# Leave empty to disable authentication on the backup UI.
BACKUP_UI_USER=
BACKUP_UI_PASSWORD=